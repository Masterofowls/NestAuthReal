# NestAuth backend (direct — Electron talks to backend, not the web proxy)
BETTER_AUTH_BACKEND_URL=http://localhost:3000

# Next.js sign-in page URL (Electron opens this in the system browser)
BETTER_AUTH_SIGN_IN_URL=http://localhost:3001
