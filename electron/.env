# Frontend URL — Electron routes all auth through the same Next.js
# /api/auth rewrite proxy that the browser frontend uses.
# Change to the production URL when building for release:
#   BETTER_AUTH_URL=https://frontend-five-gold-zmyppyq06g.vercel.app
BETTER_AUTH_URL=http://localhost:3001
